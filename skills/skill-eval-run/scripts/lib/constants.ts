/** Default timeout for agent runs and grading, in seconds. */
export const DEFAULT_TIMEOUT_SECONDS = 600;

/** Default number of parallel entries to run/grade. */
export const DEFAULT_PARALLEL_ENTRIES = 2;
